# Evaluation Log

このファイルは、課題コードを評価した記録を残す場所です。

評価基準そのものは `docs/learning/evaluation.md` に置き、このファイルには現在の状態や次に見るべき点を記録します。

## 現在の評価方針

- 最低要件とエンジニア視点を分けて評価する。
- 答えの完成コードは書かず、次に確認する1点を残す。
- 構文エラーがある場合は、まず実行可能性を最優先にする。
- 一見正しい出力でも、別の値で試したときに要件を満たすかを見る。
- 評価を行った場合は、原則としてこのファイルに記録する。
- 完成コードや最終解答そのものは記録しない。

## 評価記録テンプレート

```md
### YYYY-MM-DD 対象ファイル

- ランク:
- 最低要件:
- エンジニア視点:
- 次に確認する1点:
- 記録メモ:
```

## 2026-06-23 初期チェック

### 全体

- `works/` 配下のPHPファイルに対して構文チェックを実行。
- 多くのファイルは構文上は実行可能。
- `works/work1/q4.php` は構文エラーがあり、まず実行可能にする段階。

### work1

| ファイル | 現在の評価メモ |
|---|---|
| `q1.php` | `if / else` の形は書けている。問題文の「10より大きい」と現在の条件式の対応は再確認するとよい |
| `q2.php` | `if / elseif / else` の基本構造は書けている |
| `q3.php` | 現在の値では出力が合って見えるが、偶数判定では割り算の結果ではなく余りを見る必要がある |
| `q4.php` | 構文エラーあり。まず文の終わり方を確認して実行可能にする |
| `q5.php` 以降 | TODOが残っており、未着手または評価前 |

### work2

- ループ・配列の課題群。
- 評価時は、指定された `for` / `foreach` を使えているかを重視する。

### work3

- 関数の課題群。
- 評価時は、引数を受け取り、結果を `return` し、既存テストがOKになるかを重視する。

### work4

- 型宣言の課題群。
- 評価時は、引数と戻り値の型、`strict_types` の意味、型エラーの扱いを重視する。

### work5

- クラス基礎の課題。
- 評価時は、型宣言、privateフィールド、getter/setter、バリデーションを重視する。

### workEx1

- クラス応用の課題。
- 評価時は、状態管理、残弾数、最大装弾数、拡張マガジンの整合性を重視する。

### workEx2

- 総合問題。
- 評価時は、関数の役割分担、平均点計算、成績判定、合格者抽出のつながりを重視する。

## 次回以降の評価時に見ること

1. まず `php -l 対象ファイル` で実行可能性を見る。
2. 次に問題文と現在の条件式・処理を照合する。
3. その後、最低要件を満たしているか判定する。
4. 最後に、シンプルで読みやすいかをエンジニア視点で見る。
5. 評価コメントは、改善点を1つに絞って返す。

## 2026-06-23 work1全体チェック

### `works/work1/q1.php` から `works/work1/q10.php`

- ランク: 全体B。構文は全問OKだが、q8とq10は最低要件に修正点あり。
- 最低要件: q2からq7は問題文どおり。q8は60点の境界、q10は0を最初に確認する指定が未達。q1は10ちょうど、q9は1桁の範囲を確認するとよい。
- エンジニア視点: 条件分岐の形は読みやすく、余りを使った判定もできている。次は「以上」「より大きい」「最初に確認」のような日本語条件を式と分岐順に正確に対応させる段階。
- 次に確認する1点: まず q8 の「60以上80未満」に、60点ちょうどが含まれるかを確認する。
- 記録メモ: 完成コードは記録しない。q8とq10を直したら再評価し、work2へ進めるか確認する。

## 2026-06-23 work1修正後チェック

### `works/work1/q1.php`, `q8.php`, `q9.php`, `q10.php`

- ランク: 修正中B。q8は修正済み。q10は分岐順の考え方は改善したが、変数名の誤りが残る。q1は保存内容ではまだ境界条件が未修正。
- 最低要件: q8は「60以上80未満」を満たす。q10は0を最初に確認しているが、正の数判定で別変数を見ている。q1は「10より大きい」と「10以上」の違いが保存内容に反映されていない。
- エンジニア視点: q9は現在値では動くが、「そうでなければ」という問題文には最後をelseにする方が自然。負の数を扱うかどうかも確認余地あり。
- 次に確認する1点: q10の正の数判定で、判定したい変数名が他の行と同じか確認する。
- 記録メモ: 完成コードは記録しない。q1の保存状態とq10の変数名修正後に再確認する。

## 2026-06-23 work1再修正後チェック

### `works/work1/q1.php`, `q8.php`, `q9.php`, `q10.php`

- ランク: A。q1、q8、q10は前回指摘点が修正済み。q9は教材の正の整数想定ならおおむねよいが、負数まで扱うなら条件の再検討が必要。
- 最低要件: q1は「より大きい」に修正済み。q8は60点を合格に含める条件になっている。q10は0を最初に確認し、正負の判定変数もそろっている。
- エンジニア視点: q9で `10以上` を2桁以上とする考え方は正の数だけなら自然。ただし負数を含めると、負の2桁が1桁側に入るため、桁数を「符号を除いた大きさ」で見る発想が必要。
- 次に確認する1点: q9で負数を課題範囲に含めるか、問題文の想定を決める。
- 記録メモ: 完成コードは記録しない。q9の方針が決まればwork1は通過判断できる。
